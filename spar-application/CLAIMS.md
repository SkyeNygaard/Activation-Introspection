# Claim ledger

This ledger is stricter than the pilot READMEs on purpose. "Validated" means the
statement follows from the current code or saved pilot output **at the stated
scope**. It does not mean the result generalizes. "Exploratory" means the result
may motivate a confirmatory study. "Invalidated" means the stated inference does
not follow. "Untested" means no experiment yet estimates it.

| Claim | Status | Estimand | Independent unit | Evidence | Validity / next requirement |
|---|---|---|---|---|---|
| The activation apparatus can inject a concept vector at selected transformer layers and evaluate downstream behavior. | **Validated, apparatus only** | Behavior and readout under a specified intervention | Prompt-concept item. A training run counts only when training is stochastic and independently initialized | [activation-introspection](../activation-introspection/) | This establishes engineering capability, not introspection. Retain item-level outputs and intervention checks in the repaired run. |
| Training a model to verbalize its activations produces a reader that exceeds a probe fitted on the same states. | **Refuted on the shared axis; reversed off it. The general form of this claim is untested** | Twin-pair and row accuracy on the adapter's own held-out bank, scored on concept directions and on magnitude-matched random directions | Twin pair; 24 pairs over 8 held-out directions × 3 held-out carriers. For the random arm the independent unit is the **direction (8), not the row (48)** — see below | [notes/12](../activation-introspection/notes/12-training-versus-a-probe.md), [notes/13](../activation-introspection/notes/13-shared-axis-audit.md), [`trained_vs_probe_v2_*`](../activation-introspection/results/), [`bank_audit_v1_summary.json`](../activation-introspection/results/bank_audit_v1_summary.json), [`probe_offaxis_v1_summary.json`](../activation-introspection/results/probe_offaxis_v1_summary.json) | On concept directions both a difference-of-means centroid and L2 logistic regression reach **1.0000** twin-pair against the adapter's published **0.927**. [notes/13](../activation-introspection/notes/13-shared-axis-audit.md) then measured why: the fitted reader **is** the average training direction (overlap 0.99999) and that average points positively along **all eight** held-out directions at 0.171–0.243, against a chance overlap of 0.022 in 2048 dimensions. All 56 within-bank direction pairs are positive. The task therefore collapses to *"was this state pushed along one axis, plus or minus"* and never requires knowing the concept. Scored off that axis on magnitude-matched random directions, the same fixed readers fall to **0.479 row / 0.417 twin-pair** (centroid) and **0.438 row / 0.000 twin-pair** (logistic) — the logistic reader emits one constant label per cell — while the trained adapters hold **0.913–0.955** ([notes/08](../activation-introspection/notes/08-sensitivity-specificity-tradeoff.md)) and the untrained model sits at 0.513. The centroid's per-direction scores are all-or-nothing (`1.0, 1.0, 1.0, 0.667, 0.167, 0.0, 0.0, 0.0`), which is a coin flip over directions and the reason the independent unit is the direction. **So "introspection training is probe distillation and loses to it" is false as stated**: it holds only where the probe was fitted to be optimal. **The reversal originally recorded here has itself been retracted** ([notes/15](../activation-introspection/notes/15-matched-reader-on-content.md)). The fixed probe gets one weight vector fitted once on another bank, while the adapter gets four episode-specific demonstrations and a re-randomised convention every episode — unequal access, so the probe's collapse cannot support a claim about probing in general. The fair comparator, notes/11's four-shot reader that refits inside each episode, scores **1.0000 on the same random directions**. Per-episode adaptation, not training, is what carries the generality; what notes/13 showed is only that a *fixed* probe lacks it. **Scope, stated because the earlier version of this row overreached:** one model, one layer, one strength, one training recipe, one random-control seed, three to four seeds. Adapters were compared against frozen published numbers rather than re-scored here, and that limitation applies to the reversal exactly as much as to the original finding. Nothing here licenses any claim about training in general. |
| The model's use of its own internal state exceeds what a third party of equal or lower computational cost achieves from that same state. | **Refuted across four task structures** | Paired per-episode accuracy, model versus a four-shot nearest-centroid reader fitted inside each episode on that episode's four demonstration states, captured in the forward pass that scores the model | Episode; 288 per arm, 1152 total, plus notes/11's 576 | [notes/15](../activation-introspection/notes/15-matched-reader-on-content.md), [`matched_reader_content_v1_*`](../activation-introspection/results/) | Tested against the field's operative criterion ([arXiv 2508.14802](https://arxiv.org/abs/2508.14802)). Model versus reader: **content 0.899 vs 1.000**, **polarity 0.917 vs 1.000**, **random ± 0.663 vs 1.000**, **weak (strength 0.15) 0.497 vs 0.833**. Paired model-only counts are **0, 0, 0 and 14** against reader-only counts of 29, 24, 97 and 111. Combined with notes/11's 576 episodes, that is **14 episodes in 1728 where the model succeeds and a two-centroid comparison fails**. The weak arm reproduces notes/08's blind base model at 0.4965 against its reported 0.500, an internal check on the harness. The reader sits at 0.833 there, **inside** the 0.790–0.863 range notes/08 reports for the trained adapters — so training lifts the model to roughly where the cheap reader already was rather than past it, though that is two numbers from two runs and not a paired test, since the adapters were never saved and cannot be re-scored. **Scope:** one model, one layer, four concept pairs, one control seed. The reader is scored only on the state-to-label decision while the model also parses the prompt and emits a formatted token, which it does at format rate 1.000. The reader reads at the injection site; notes/11 tested that across all depths for polarity and found it perfect over 25 consecutive blocks, and that sweep has not been repeated for content. The shuffled-label control ran at 0.417–0.601 against an expectation of 0.500 because the permutation is seeded by episode index, giving only 24 distinct permutations reused across pairs — disclosed, and no gate turns on it. |
| The model reads the *content* of an injected state, not merely that a state was disturbed. | **Validated at the stated scope, for injected content** | Accuracy and twin-pair accuracy where the two classes are two different concepts rather than one concept and its negation, at separation between classes equalized across arms by construction | Episode (288 per arm); twin pair (144 per arm). Four concept pairs | [notes/14](../activation-introspection/notes/14-content-versus-disturbance.md), [`content_vs_disturbance_v1_*`](../activation-introspection/results/) | Changing the two classes from `+v`/`−v` to `v_A`/`v_B` and changing nothing else, the model scores **0.899 accuracy / 0.799 twin-pair**, against a polarity anchor of 0.917/0.833 — while **two random directions at identical separation score 0.594/0.188**. Content beats random on **4 of 4 pairs** (margins +0.320, +0.111, +0.403, +0.389) and is within 0.042 of the polarity anchor on every pair. Separation is matched by construction: strength is `2 / ‖unit(a) − unit(b)‖` per pair, giving 1.000 for opposite poles, 1.444–1.492 for concept pairs and 1.393–1.423 for random pairs, so the distance between classes is 2.0 in every arm and a drop cannot be attributed to the classes being closer together. Structural controls land exactly on their construction values: `query_only` **0.5000** (twin-pair 0.014) and `clean` **0.5000**, with format 1.000 and label mass ≥0.997 throughout. Because the two classes are two different concepts, this task **cannot be solved by projecting onto a single axis**, which is what [notes/13](../activation-introspection/notes/13-shared-axis-audit.md) showed the earlier task collapses to. Both effects are real: random pairs sit *above* chance, so a generic disturbance component exists, and content adds +0.305 accuracy and +0.611 twin-pair on top of it. **Scope: this is a capability claim, not a privileged-access claim.** The matched-cost reader comparison that [notes/11](../activation-introspection/notes/11-matched-cost-reader.md) applied to the polarity task has **not** been run here, so nothing licenses calling this introspection. Content is injected, not naturally computed. Four pairs, one model, one layer, one strength; the polarity arm uses four of the eight concepts behind the published 0.891 and is an internal anchor, not a reproduction of it. |
| The concept banks behind the reporting results measure identification of *which* concept is active. | **Refuted for the polarity task; see the row above for the content task** | Pairwise overlap of centred concept directions, and overlap of each fitted reader weight with each held-out direction | Direction pair (56 within-bank); direction (8 for the reader comparison) | [notes/13](../activation-introspection/notes/13-shared-axis-audit.md), [`bank_audit_v1_summary.json`](../activation-introspection/results/bank_audit_v1_summary.json) | Centering was estimated from eight concepts and did not remove the shared component. Within-bank overlaps average 0.096 (training) and 0.097 (held-out) with **28 of 28 pairs positive in each**, against a chance value of 0.022; the average training direction and the average held-out direction overlap at 0.480. The bank passed the apparatus's own admission screen, which rejects overlaps above 0.5 and saw a maximum of 0.183, but `center_bank`'s own docstring sets the standard for identification claims higher — *"if the off-diagonal is not near zero, no identification result is valid"* — and 0.096 is not near zero. **The bank was screened against the wrong threshold.** What the reporting numbers in notes/06, 07, 11 and 12 measure is detection along a single direction, not semantic content. The arithmetic of those results is untouched and the structural argument that an input-only learner is pinned at 0.500 by construction is untouched; only the interpretation changes. Fixing this requires a bank whose directions are near-orthogonal after centering, which no run in this repository currently has. |
| The model's in-context report of a causally injected hidden state exceeds what a third party of equal or lower computational cost achieves from the same state. | **Refuted at this site, by a complete dominance relation** | Paired per-episode accuracy, model versus a four-shot nearest-centroid reader given the five post-injection residual states captured in the same forward pass | Episode; the identical 576 episodes that produced the 0.891 | [notes/11](../activation-introspection/notes/11-matched-cost-reader.md), [`matched_reader_v3_*`](../activation-introspection/results/) | Tested against the field's operative definition ([arXiv 2508.14802](https://arxiv.org/abs/2508.14802)): introspection requires being more reliable than an equal-or-lower-cost third party. The model reproduced its frozen confirmation at **0.8924** (gate 0.891 ± 0.05) and the reader scored **1.0000** on both centroid variants and all eight concepts, with the shuffled-label control at 0.5017. Paired: **514 both correct, 62 reader-only, 0 model-only** — there is no episode where the model succeeds and the cheap reader fails. **This does not retract the 0.891**, whose structural contribution (an input-only learner pinned at exactly 0.500 by construction) is untouched; it retracts any introspection, privileged-access or self-knowledge reading of it. The pre-registered interpretation was written before the run and states the expected direction is unflattering to the headline. The injection-site objection was then tested adversarially by refitting the reader after **every** block ([`reader_depth_v1_*`](../activation-introspection/results/)): blocks 0–8 give exactly 0.5000 (validity control), blocks **9–32 give exactly 1.0000**, block 33 gives 0.9427, and only blocks 34–35 fall below the model. The reader beats the model at **25 consecutive read depths, ~70% of the network**, so the verdict broadens rather than weakens. That protocol's own frozen verdict fired as `dominance_is_a_read_site_artifact` because it named the final block as primary — a **badly chosen statistic**, since by the last blocks the marker positions are producing their own next-token predictions rather than storing the concept; the error is disclosed in the note rather than overridden. Remaining limits: the state is **planted**, not naturally computed; the reader is scored only on the state-to-label decision; one model/layer/strength is not a general failure. A trained reporter has **not** been measured this way, and that comparison is the difference between introspection training and probe distillation. |
| Replacing the naturally computed residual at the position that produces the answer, with its matched twin's, makes the ordinary answer follow the donor state. | **Located, reproduces on development data, and failed its held-out confirmation** | Unrestricted next-token answer under bidirectional residual interchange | Five arithmetic twin pairs on a development bank; a feasibility pilot, not population inference | [`natural_state_arith_smoke_v1_*`](../activation-introspection/results/), [`natural_state_arith_site_diagnostic_v1.json`](../activation-introspection/results/), [notes/10](../activation-introspection/notes/10-output-ready-arithmetic.md) | The successor to the row below, built to remove that row's two confounds. The clean task is perfect — 10/10 answers at conditional probability 1.000 — and exact self-patching reproduces the full logit vector with maximum error 0.0. At the three prospectively named anchors the transplant controlled the answer in **0/5 tasks**, recovery +0.001/−0.003/+0.100 against a frozen 0.5 threshold, and the frozen stop fired before any reporting row. A **post-hoc, development-bank-only** all-layer localization then explains it: through block 26 the clean state does not favour its own answer over its twin's better than chance under a logit lens (sign correct on 5/10 problems) and the twins differ by under a quarter of the residual norm, so there is nothing to transplant; from **block 27** the donor's digit is the full-vocabulary argmax in **10/10** transplants at recovery 0.787. The anchors were one block short. A third protocol then froze block 27 as a disclosed post-hoc site with everything else unchanged: development reproduced at **5/5, recovery 0.787**, and the never-scored held-out bank returned **3/5 against a frozen 4/5** at recovery 0.697, so the reporter did not run and reselection is forbidden. Eight of ten held-out transplants worked and both failures were the same direction; pooled over both banks the per-transplant rate is **0.90**, at which a five-task "4/5 in both directions" criterion fails about a quarter of the time unaided — the binding defect is bank size, and that criticism was made after seeing the failure and does not convert it into a pass. **No reporting number exists from any of the three runs.** Status is not "validated" and not "refuted": the site is real on development data and unconfirmed out of sample. Next requirement is a fresh protocol with a third bank of 10–20 pairs, or a donor column across blocks 27–33 rather than a single block. |
| Replacing the naturally computed route-marker residual after layer 9 with its matched twin makes the ordinary two-hop answer follow the donor state. | **Tested and stopped at this exact site; superseded by the row above** | Unrestricted next-token answer under bidirectional residual interchange | Five route worlds; this is a feasibility pilot, not population inference | [`natural_state_smoke_v2_*`](../activation-introspection/results/), [notes/09](../activation-introspection/notes/09-natural-state-pilot.md) | Exact self-patching reproduced the full logit vector with maximum error 0.0, but clean task capacity was 8/10 and 0/5 worlds followed the donor bidirectionally. The frozen gate stopped before any reporting row. This is an instrument no-reach result, not evidence against natural-state reporting. Reopen only after disjoint-DEV token/layer localization passes on a reliable task, then freeze a fresh report bank. The saved normalized-recovery diagnostic is invalid because negative clean margins were divided by a positive floor; that bug did not affect either failed gate. |
| In `Qwen2.5-3B-Instruct`, four demonstrations with visible observation content held fixed but causally different residual states teach an episode-specific `Q/K` codebook in context. In the frozen V2 repair-confirmation, target accuracy is 0.891 [0.816, 0.995], versus 0.500 for the exactly scale-matched query-only arm; target minus the strongest random/shuffled direction is +0.231 [0.137, 0.286]. | **Validated at the stated scope; extension candidate** | Opaque-label accuracy from a causally varied query state after four causally labeled hidden-state demonstrations | Eight concept directions unused in DEV/V1 crossed with three fixed carrier strings, independently resampled in an exact crossed-bootstrap distribution. The 24 enumerated order × map × query cells are nuisance marginalization | [protocol and report](../activation-introspection/notes/06-causal-codebook-icl.md), [`results/codebook_icl_confirm_v2_*`](../activation-introspection/results/) | V1 was inspected and retained after audit found normalization, centering, provenance, and interval defects. V2 kept all tuned settings/gates fixed; a post-freeze two-cell smoke scored target 2/2 and caused no retuning/stopping decision. Query twins have byte-identical visible prompts and opposite correct labels; target gets both right on 0.781 [0.632, 0.990], while query-only gets both on 0.035 [0.000, 0.080]. Next-token format integrity is 1.000 in every arm. `clean = 0.500` and aggregate `test_only = 0.500` are arithmetic; the paired contrast is evidence. Random and shuffled directions are themselves learnable at 0.658/0.660, so the broad phenomenon is generic hidden-state codebook learning; the target advantage is direction-specific but not proven semantic because controls are per-position magnitude-matched rather than downstream-damage-matched. The +0.231 contrast was preplanned but not a frozen gate with a smallest effect size of interest. This does not establish privileged self-access, J-space structure, training transfer, natural-state reporting, safety-monitor robustness, or population robustness. A targeted search found no exact causal matched-visible episode-remapped ICL design, but that licenses “extension candidate,” not “first.” |
| A paired all-head attention-output interchange identifies downstream receiver-role candidates for the causal-codebook effect. | **Validated DEV selection; superseded by the Stage 1b stop** | Fraction of the aggregate target–query-only signed-margin gap removed by a paired donor patch | One DEV concept × one carrier; 12 balanced nuisance cells | [`attention_localization_dev_v2_*`](../activation-introspection/results/) | The frozen screen selected query-marker L21/L23 (0.343/0.253 removal) and final-answer L26/L31 (0.887/0.417). Format is 1.0 and label-mass retention exceeds 0.9998. It does not isolate heads, QK, or edges; roles overlap, layer 26 has substantial KL, and unexplained all-position paths remain. The separately frozen disjoint-DEV head screen it authorized has now run and stopped; see the next row. Three of these four layer-role pairs replicated on fresh concepts, but `query_marker@23` did not. |
| Training an activation reporter extends its detection floor to injected edits far weaker than the untrained model can read. | **Validated at the stated scope; three seeds** | Row accuracy and paired accuracy on held-out concept directions and carriers, at injection strengths at and below the training strength | Training seed crossed with held-out concept and carrier; the 24 order × mapping × query-sign cells are nuisance marginalisation | [notes/08](../activation-introspection/notes/08-sensitivity-specificity-tradeoff.md), [`remap_training_v2_*`](../activation-introspection/results/), protocol SHA-256 `f29b479d…be0f0` | Trained only at strength 0.5, both adapters read strength 0.25 at 0.990–0.997 against base 0.526, and strength 0.15 at 0.790–0.863 against base **0.500 exactly** with 0.010 twin-pair accuracy — the untrained model is blind there. All three seeds positive at every strength; format 1.00 and label mass 1.000 in every arm. Mapping-flip pairs move with it (base 0.438 against 0.736–0.806), so this is state reading plus in-context rebinding, not a sign probe. Strength was fixed on a DEV concept before any held-out bank was touched, after a strength-1.0 pilot came out at ceiling with near-zero training loss. A weaker planted direction is still planted: this is one step along the naturalness axis, not arrival at it, and the floor is bounded below 0.15 rather than located. Three seeds give a mean and a range, not an interval. |
| A model's confidence in its own self-report carries usable information about whether that self-report is correct. | **Validated at the stated scope, and it reverses [notes/20](../activation-introspection/notes/20-comparator-tiers.md)** | AUROC of the model's answer margin against its own correctness, on held-out seeds | Episode; 2,016 rows across four training seeds, split development (0,1) / confirmation (2,3) before any was opened | [notes/29](../activation-introspection/notes/29-can-abstention-recover-selectivity.md), [`selective_introspection_v1_summary.json`](../activation-introspection/results/) | Confirmation AUROC **0.675** (base) and **0.891** (trained); the trained arm reaches 1.000 accuracy at 70% coverage from 0.969 at full coverage. **notes/20's "calibration is unusable" is withdrawn**: it compared mean confidence when right (0.998) against when wrong (0.928) and read the small gap as absence of signal, but that is a difference of means on a quantity squashed against 1.0, and the ordering was informative the whole time. **Scope:** confidence here is an internal logit margin, not a number the model states, so a monitor built on it needs activation access. Secondary analysis of artifacts frozen for a different purpose. One model, one recipe. |
| Letting a trained introspective reporter abstain on its least-confident reports recovers the selectivity that training destroyed. | **Refuted, and the effect runs the other way** | Gap between accuracy on real concept directions and on magnitude-matched random directions, as the least-confident rows are dropped | Episode; 768 rows per arm/condition on confirmation seeds 1 and 2, development seed 0 declared first | [notes/29](../activation-introspection/notes/29-can-abstention-recover-selectivity.md), [notes/08](../activation-introspection/notes/08-sensitivity-specificity-tradeoff.md) | Dropping to 20% coverage moves the gap from 0.059 to **0.013** (`fixed`) and 0.099 to **0.019** (`remap`), while the untrained model moves the opposite way, 0.232 to **0.455**. So a trained reporter is most confident precisely where the injected direction was meaningless: training makes it confidently wrong, which is the worst arrangement of those two properties. Anthropic's [Introspection Adapters](https://arxiv.org/pdf/2604.16812) names "abstention mechanisms" as one of three candidate fixes for exactly this false-positive rate; on this recipe it fails. **Scope:** one model, one training recipe, the strength-0.5 cell only, secondary analysis. It does **not** test the DPO-refined adapters that paper actually proposes. A separate development-pass signal — that the margin might still separate concepts from noise — **failed to confirm** (0.815/0.769 dev against 0.612/0.722 confirm) and is reported as failed. |
| Training an activation reporter destroys its semantic selectivity. | **Validated at the stated scope; three seeds** | Row and paired accuracy on magnitude-matched random directions, against concept directions, before and after training | Training seed crossed with held-out concept and carrier | Same artifacts | Untrained, the model is at chance on random directions (0.513 row, 0.026 twin pairs) while reading concept directions at 0.745 — its access is selective. After training on eight concept directions, random directions reach 0.913–0.955. The safety reading is that such a monitor answers "did something move at this site", not "is concept X active", so any displacement yields a positive report. This was a post-hoc observation in the v1 artifact and was declared as gate D in the v2 protocol before any v2 data existed; it passed prospectively on all three seeds. Controls are magnitude-matched, **not** downstream-damage-matched, so this does not separate semantics from causal potency. |
| Training a reporter under a fixed label convention damages its ability to adopt a new convention supplied in context. | **Invalidated by its own pre-registered test** | Row, query-twin and mapping-flip accuracy of a fixed-convention adapter against the untrained model | Training seed | [`remap_training_v1_*`](../activation-introspection/results/), summary carries `gate set v1` and `all_gates_pass=false` | This was the hypothesis protocol v1 was built to test. Two seeds refuted it: both adapters reached 1.000 on row, twin and mapping-flip accuracy against base 0.745/0.490/0.677, and the fixed adapter was marginally *better* at low strength. The artifact is retained under its own gate set rather than re-run under the later protocol's gates. A tooling flaw was found and fixed in the process: gates live in analyzer code rather than in the protocol JSON, so editing the analyzer for v2 silently re-judged the v1 artifact from fail to pass. Gate sets are now keyed to the protocol that produced the artifact. |
| A LoRA trained on one bank of concept directions verbalizes the sign of a causally injected hidden state on directions and carriers withheld from training. | **Validated at the stated scope; four seeds** | Twin-pair accuracy: a concept-carrier cell counts only when both byte-identical members receive their opposite labels. The independent unit is the training seed | Four training seeds, each over eight held-out concept directions crossed with three held-out carriers | [notes/07](../activation-introspection/notes/07-trained-activation-reporter.md), [`report_training_v3_*`](../activation-introspection/results/) | Trained target 0.927 mean, range [0.833, 1.000] across seeds 0-3, against 0.000 for the untrained base model on every seed and 0.260/0.208 for magnitude-matched random and shuffled directions. Format rate and label mass are 1.000 throughout. Every seed passes every gate and beats its own strongest control by +0.542 to +0.708. **The pair-wise null is 0.000 for a prompt-only strategy and 0.250 for a coin flip, not 0.500**; the frozen 0.500 threshold is conservative. Generalization is strong: 1.000 on the adapter's own eight directions against 0.927 held-out. Four seeds give a mean and a range, **not an interval** — they share one model, one layer, one strength, one concept bank, and one machine. The mapping is fixed and global rather than episode-remapped, so this is not comparable to the 0.891 in-context number and that figure is not its baseline. This is not privileged self-access, not verbalization of naturally occurring computation, and not faithfulness under adversarial pressure. |
| Restricting an introspection-training loss to the answer options yields a discriminative readout that is not a verbalization. | **Validated as a methodological finding** | Full-vocabulary format rate and label mass of a trained reporter, alongside its forced-choice accuracy | The V1/V2 pair differing only in the training loss | [`report_training_v1_*` and `_v2_*`](../activation-introspection/results/) | V1 minimized cross-entropy over the two label logits alone. It reached 0.917 twin-pair accuracy on held-out directions and passed all three gates that existed at the time, while holding ~5e-9 total probability on the two labels and a 0.000 full-vocabulary format rate: the model never emitted a label. No forced-choice metric can detect this, because it is invisible inside the option set — only an unrestricted next-token check finds it. A verbalization gate was added to the analyzer afterwards and V1 now reports `gates_pass=false`. **The repair costs nothing in accuracy**: the seeded V3 runs reach 0.927 with format and mass at 1.000, against V1's degenerate 0.917. This is a single controlled comparison at one setting, not a general law about training losses. |
| The 0.917 to 0.583 drop between V1 and V2 measured how much the broken loss inflated the result. | **Invalidated / retracted** | Effect of the loss repair on held-out twin-pair accuracy | Training seed | Superseded by [`report_training_v3_*`](../activation-introspection/results/) | Stated in this repository on 2026-08-10 and wrong. V1 and V2 seeded only the example order, never the adapter initialization, so neither run could be distinguished from initialization luck and V2's 0.583 was one uncontrolled draw. Four seeded runs give 0.927 [0.833, 1.000], all above V2. The repair cost essentially nothing in accuracy. This is the same pseudo-replication error the ledger already records twice elsewhere, made again in the same session as the V1 write-up that diagnosed a different defect. One limit on the diagnosis: seeding fixes adapter initialization *and* makes LoRA dropout deterministic, so V2 and V3 differ in two ways and four seeds cannot separate a low draw from a systematic effect of determinism. What is certain is that V2 alone could not support a number. |
| The causal-codebook effect replicates on concepts and carriers used in no tuning or selection step. | **Validated at the stated scope** | Opaque-label accuracy, target minus the exactly query-matched arm | Three concepts crossed with two carriers, all disjoint from the V2 confirmation bank and the Stage 1a screen; 12 balanced nuisance cells each | Baseline arm of [`attention_head_screen_dev_v3_summary.json`](../activation-introspection/results/) | Target accuracy 0.958 against 0.500 for `test_only`, mean signed-margin gap 5.19, format 1.000, correct sign in 6/6 strata. This arose as the control arm of an experiment commissioned for a different purpose, so it is a genuine out-of-bank check rather than a confirmatory rerun. It inherits every scope limit of the V2 claim: one model, one layer, one strength, binary labels, large out-of-distribution edits. No interval is quoted because six strata are too few for the crossed bootstrap used in V2. |
| A compact set of individual attention components mediates the causal-codebook effect. | **Tested and stopped by its own pre-registered gate** | Fraction of the aggregate target–query-only signed-margin gap removed by a paired single-head donor patch | Three concepts × two carriers, disjoint from Stage 1a; 12 balanced cells each | [`attention_head_screen_dev_v3_*`](../activation-introspection/results/), protocol SHA-256 `759c0850…25856d` | 5,112 scored forwards over all 64 layer-role-head components. `proceed_to_separately_frozen_stage1c` is **false** on two gates: `query_marker@23` removed 16.2% against a 20% parent threshold, and six components qualified against a frozen 2–4 window. The three `final_answer@26` heads together exceed 110% of their parent's 71.6% removal, so they are redundant, not decomposable. **The screen has no zero/random-donor damage control**, so removal fraction cannot be separated from perturbation disruptiveness; format and label mass sat at ceiling for every component and do not substitute. That control was deferred to Stage 1c, so a passing screen would have been equally uninterpretable — a gate-ordering error, recorded against the next attempt. The stop does not show no compact route exists: only single query heads at four preselected layer-role sites were tested. |
| One released GPT-2 positional attention program has an exact `O(TD)` lowering that is numerically equivalent on the frozen grid and faster as an isolated operator, but not as an integrated module. | **Validated at the stated scope; the deployment gate failed** | Paired median latency ratio, dense against lowered, at fixed batch/length on CPU fp32 | Timing block; 30 paired blocks per cell | [`programmatic_attention_v1_*`](../activation-introspection/results/), protocol SHA-256 `7ad77347…96f593` | 216/216 equivalence cells pass at max abs error 4.8e-7. Isolated mixer at `B=1, T=1024`: 18.63× [18.45, 18.83]. Native-pruning integration into the real GPT-2 attention module: 1.089× [1.088, 1.091], against a 1.25× threshold frozen before the grid ran. The informative content is the collapse: the algebra is not the bottleneck, partial-head projection and dispatch are. This is CPU-only, one program, one model, forward-only, uncached, no allocator peak-memory measurement, and it is not a behavioral-fidelity or end-to-end speedup claim. It is reported as project evidence rather than an appendix because the project's stated question is cost to task performance **and** efficiency. |
| A fixed programmatic QK route improves the causal source-faithfulness of activation reports under surface-cue reversal. | **Untested safety protocol** | Hidden-intervention effect, cue-swap effect at fixed hidden state, reversed-cue worst-group accuracy, and programmed-edge necessity | Independent reporter-training seed crossed with held-out concept and prompt family | [safety protocol](EXPERIMENTS.md#safety-north-star-causally-auditable-activation-reports-under-shortcut-pressure-untested) | V2 establishes only hidden-state codebook use. DEV layer-role localization is a selection stage, not confirmation evidence. No program, shortcut-robustness result, or safety claim exists yet; runtime and memory are not evidence for this claim. |
| In `Qwen2.5-0.5B-Instruct`, a concept injected into a neutral carrier's KV cache is causally used to answer a codebook query revealed **after** the hook is removed. Accuracy is 0.500 against 0.125 chance at layer 2, and the effect is gone by layer 14. | **Validated at the stated scope; labeled a replication** | Post-codebook label accuracy, target arm minus the strongest control arm | Concept × carrier family, cluster-bootstrapped. Codebook permutations are nuisance | [notes/05-retained-trace.md](../activation-introspection/notes/05-retained-trace.md), `results/retained_test_qwen05b_*` | Strength was frozen on a separate dev bank. The held-out bank was run initially, then rerun under a disclosed repair to the `random` control; the target rows are bit-identical and `_v2_` is the cited artifact. Gates passed: plain-text ceiling 0.875, the effect survives restriction to format-intact trials (0.435), 6 of 8 concepts clear twice chance, and the `shuffled` control sits at 0.125. Layers 18 and 22 fall inside the preregistered ±0.05 equivalence band. **Two stated gates do not count and are withdrawn.** `clean` and `sham` at exactly 0.125 is an arithmetic identity, not a passing test. One forward per (carrier, codebook) is scored against all eight concepts, and the codebooks are cyclic, so exactly one row of eight is correct however the model behaves. That holds in 144 of 144 cells. The arms are also **not** damage-matched at the quoted L2 cell: target carrier KL is 1.50 against 1.00 for `random` and 1.37 for `shuffled`. That is the same direction as the 3B layer-3 cell disqualified below, though the gap is far smaller. Format integrity runs the other way (target 0.44 against 0.21 and 0.33), so the controls break formatting *more*. The effect survives on the one control that could have failed. **This licenses causal use of a retained trace only.** It does not license introspection, self-knowledge, or privileged access. The schedule is Lindsey's, and the early-layer-only profile is already published for Llama-3.1-8B (arXiv 2512.12411). See [LITERATURE-BOUNDARY.md](LITERATURE-BOUNDARY.md). |
| The depth at which the retained trace stops being usable moves later with model scale (0.5B, 1.5B, 3B). | **Exploratory; the depth null itself is now strength-robust** | Post-codebook label accuracy against injection site as a fraction of model depth | Concept × carrier family, within model | `results/retained_test_qwen{05b,15b,3b}_*` | A dev-bank strength sweep on 2026-08-05 replaced the blanket "not damage-matched across scales" caveat with a measurement. Mean target carrier KL against the 0.5B run's 1.105 is as follows. **1.5B at `a=1` is 1.17, a ratio of 1.06, so it is matched.** The carried-over strength was in fact correct there. **3B at `a=1` is 1.98, a ratio of 1.80, so it is not matched.** Per layer it ranges 3.49, 1.39, 1.08, so no single strength damage-matches across depth at 3B. The value that would lies between 0.5 and 1 and was not sampled. Carrier KL is not even monotone in strength at layer 3. Per-layer selection is the right instrument there. **Separately, the under-injection objection is closed at both scales.** 1.5B at 93% depth stays at chance across a 76-fold carrier-damage range (KL 0.20 to 15.20). 3B at 92% depth stays at chance across a 118-fold range (KL 0.13 to 15.36). So the depth collapse is not an artifact of the edit being too weak by the time it arrives. Mid-depth behaviour differs by model and is not generalised. Three points is a direction, not a scaling law. The 3B **layer-21** cell is the strongest evidence in the portfolio. Both control arms carry *higher* carrier KL than the target arm there (1.83 for `random` and 2.91 for `shuffled`, against 1.22) and still sit at chance. That cell cannot be explained by the concept edit simply being more disruptive. Layer 15 was previously quoted alongside it and does **not** qualify, because `random` carries 0.44 against the target's 1.23, so only `shuffled` is above. The 3B layer-3 cell has the opposite problem (target KL 2.44 against controls at 0.51 and 0.41) and should not be quoted. |
| The injected concept stays linearly decodable from the retained carrier state at every injection depth, while behavioral use collapses to chance. | **Validated at the stated scope** | Transfer accuracy of a probe fit only on natural text, read from the same retained state that produced the behavioral answer | Concept × carrier family | Same run as above | Storage is 0.958 to 1.000 at **readout layer 22** for every injection site. The `shuffled` control sits at 0.125, and readout below the injection site is exactly 0.125. Storage is not uniform across readout depth. At readout 14, an injection at layer 2 reads 0.458. So the claim is "recoverable at readout 22 from every injection depth", not "at every depth". The inject-22/readout-22 cell is excluded, because capture is registered after the intervention on the same block, so it reads the edit with no intervening computation. This localizes the failure to readout and routing rather than retention. **It is not the additive-probe artifact.** A propagation control (`analyze_retained.py --cell`) rebuilds the clean readout-22 state plus the same `α·u_L` delta, with no forward computation in between. It scores 0.167 for injections at layers 2 through 14 and 0.333 at layer 18, against 1.000 for the real arm. The delta alone scores 0.125 to 0.375. The natural-text boundary does not recognize the injected vector, so the intervening blocks have to transform it first. The general decodability/use dissociation is already established (arXiv 2604.22128, 2607.23379). What is contributed here is that both sides are measured on the identical state at matched sites, which the retracted `r = -0.774` comparison was not. |
| Post-IFT performance worsens for late injection sites **because less forward computation remains**. | **Causal interpretation invalidated; descriptive pattern exploratory** | Effect of remaining nonlinear computation on post-IFT accuracy, distinct from trainable downstream capacity and hook placement | Held-out prompt-concept item, clustered within model and training run | [activation-introspection README](../activation-introspection/README.md) and corrected pilot audit | The saved decline does not identify remaining compute. An L1 counterexample breaks a simple depth account. The post-block hook means a late injection may have no downstream trainable LoRA module, so the trainable path changes with layer, not compute alone. Test pre-block against post-block hooks, and match downstream trainable capacity, including a trainable output readout. |
| Probe decodability anticorrelates with post-IFT usability, `r = -0.774`. | **Invalidated / retracted** | Correlation between decodability and post-IFT accuracy | Layer was used as the point, but the two series used different injection sites | Corrected audit of [activation-introspection](../activation-introspection/) | The negative correlation compared inject-at-8/probe-at-L against inject-at-L/post-IFT. The layer trends were mechanically mismatched. Do not quote this value as evidence. |
| "Decodability is not usability." | **Invalidated in the tested comparison** | Association between site-matched reach-to-output and post-IFT accuracy | A confirmatory unit would be a held-out item nested in an independent training run, not a layer | Corrected audit of [activation-introspection](../activation-introspection/) | A local reconstruction reversed the sign of the old comparison, so the negative headline cannot stand. The reconstruction joins an unprovenanced reach aggregate to legacy IFT output that is invalidated by active dropout, repeated pseudo-units, fixed answer mapping, and cross-layer vector reuse. It is a debugging clue, not positive evidence. The defensible conclusion is only that sites and generating runs must match. |
| Matched inject-at-L reach-to-output tracks post-IFT accuracy in the audited pilot aggregates. | **Not evidence; audit diagnostic only** | Correlation of matched reach-to-output with post-IFT accuracy | Held-out item nested in training run. Layers are ordered dependent sites | Local reconstruction in [activation-introspection](../activation-introspection/) | Several positive correlations were computed during the audit, but neither side of the join is a provenance-complete valid result. Do not quote these values in an application. Regenerate both sides from raw item-level records under the repaired code, and summarize across independent training runs. |
| Representational alignment removes, or fails to remove, an own-model activation-reporting advantage. | **Untested discovery protocol** | Symmetric own-minus-aligned-other target-control effect, `θ`, plus the raw-to-aligned reduction | Independent reporter-training run crossed with held-out concept and prompt family. Both sibling directions reported | [Study 2](EXPERIMENTS.md#study-2-does-an-own-model-advantage-survive-representational-alignment) | Study 1 must first validate the reporting interface. Confirmation requires source-blind balanced training, cross-fitted frozen maps, matched gates on decodability, reconstruction, KL, damage and format, and a residual above the SESOI in both directions. Even a positive result means residual compatibility under the tested transform, not metacognition. |
| In the legacy scripted sandbox, safety under silent, generic and specific feedback was `1.00/0.10/0.00` over the advertised 30-seed, 12-episode sweep. | **Legacy mechanism output; superseded for inference** | Horizon-12 non-leak rate for the old hand-written attacker and rule monitor under the old task generator | Legacy run seed | [saved sweep output](../adaptive-monitor-sandbox/results/sweep_output.txt) and [lab notebook](../adaptive-monitor-sandbox/notes/03-lab-notebook.md) | The policy explicitly mapped feedback to escalation rungs, so this was an apparatus check, not model evidence. Boundary bootstrap intervals such as `[1,1]` were invalid uncertainty intervals. The environment invariants are repaired and unit-tested, but these saved values are incompatible with the repaired source and need a clean mechanism rerun. |
| More informative refusals cause real models to obfuscate in context. | **Untested** | Change in covert or alternate-route behavior caused by refusal content | Independent scenario or item, with model rollout nested within item | Current [LLM menu scorer](../adaptive-monitor-sandbox/src/sandbox/llm_agent.py) | The current scorer is one-turn and forced-choice, and it supplies the exploit in the menu. It does not implement the sandbox agent protocol or execute an attack. Use relevant and irrelevant-reason controls, and a real multi-turn agent. |
| "No model up to 3B adapts", so the mechanism is absent or bounded at that scale. | **Invalidated as an inference** | Specific-minus-silent change in strategy preference across a target population of tasks and prompts | The study needs independent scenarios. Current "seeds" only resample menu order | [aggregate JSON](../adaptive-monitor-sandbox/results/llm_agent_adaptation.json) and [menu code](../adaptive-monitor-sandbox/src/sandbox/llm_agent.py) | Deterministic log-prob scoring used six groups of eight permutations, and the 48 draws contain only 22 of 24 unique orders. Stored bounds are descriptive quantiles of six permutation averages, not confidence intervals. No raw logits, shift interval, SESOI, equivalence test, or prompt population is saved. |
| Small Qwen models show strong menu-position effects in the forced-choice prompt. | **Validated as a descriptive verification at the stated scope** | Choice mass by menu slot, averaged over the finite set of orders | Menu order is a nuisance condition, not a model replicate | [`results/llm_agent_exact_raw.jsonl`](../adaptive-monitor-sandbox/results/llm_agent_exact_raw.jsonl) plus paired summary, and the [exact-order code](../adaptive-monitor-sandbox/src/sandbox/llm_agent.py) | Exact enumeration is the appropriate control, rather than random "seeds". **The artifact is now retained**: 72 rows, all 4! = 24 orders exactly once per feedback level, `Qwen2.5-0.5B-Instruct` revision `7ae5576…`, raw SHA-256 `bd1d9239…8cbe4`. That is bit-identical to the 2026-08-01 temporary run recorded in [AUDIT-MANIFEST.md](AUDIT-MANIFEST.md), so the deterministic scoring reproduces across processes and dates. Display slot 3 takes 0.627, 0.672 and 0.648 of the mass under silent, generic and specific feedback, while slot 1 takes 0.094, 0.045 and 0.063. That is a **9× to 15× position effect**. Once order is marginalised, option identity is near-uniform (0.228 to 0.262 everywhere) and the specific-minus-silent shifts are at most 0.009. Those shifts are descriptive only. Menu orders are not independent samples, so this is **not** a null or equivalence claim about adaptation. See the retraction two rows above, which this reinforces: a nuisance roughly 70 times the size of the contrast makes any 6-of-24-order subsample arbitrary. Generalization still requires multiple items and prompt templates. |
| The legacy sandbox implemented an attack staged in one episode and exfiltrated several episodes later. | **Invalidated for saved results; invariant repaired and tested** | Delay between staging and boundary crossing | Attack opportunity | [environment source](../adaptive-monitor-sandbox/src/sandbox/env.py), [state source](../adaptive-monitor-sandbox/src/sandbox/state.py), and tests | The saved run published a public write at that episode's end. The current source requires causal acquisition and a later-episode publisher, with timeline tests. That validates an environment invariant only. No replacement attack result has been run. |
| Unbounded provenance gave legacy safety `1.00` at reported honest usefulness of about `0.49`. | **Legacy estimate invalid; task accounting repaired and tested** | Horizon-12 safety and honest task completion under the synthetic generator | Scenario or run | [environment source](../adaptive-monitor-sandbox/src/sandbox/env.py) and tests | Persistent outputs let repeated tasks count as complete despite a block. Current code requires successful current-episode operations. The old point estimate is not transformed into a repaired value. Only a fresh run can estimate one. |
| The local 1.5B LLM monitor is no better than the rule monitor in aggregate. | **Invalidated as a comparison** | Difference in detection at a fixed false-positive budget | Held-out scenario or run | [lab notebook](../adaptive-monitor-sandbox/notes/03-lab-notebook.md), [metric code](../adaptive-monitor-sandbox/src/sandbox/metrics.py) | The notebook reports action-level LLM counts but compares them against episode-maximum rule metrics. Attacker-run routine actions are labeled positive. Calibration and test separation are absent, as are clustered intervals. Raw scores and a reproduction script are not tracked. |
| A finite monitoring window has no useful knee and only buys delay. | **Exploratory; rerun and survival analysis required** | Censoring-aware time-to-leak, and safety and usefulness under each memory window | Independent scenario or run | [window monitor](../adaptive-monitor-sandbox/src/sandbox/monitors.py) and legacy output | The script retries until hard expiry, so part of the pattern is structural. The legacy mean treats horizon-censored runs as observed leak times, and it predates the environment and provenance repairs. It is not validated even for the current script. |
| Valid first-refusal feedback increases functional harm under a fixed monitor, and private monitor memory attenuates that effect. | **Untested discovery protocol** | First `τ_fixed = P(harm|q=1) − P(harm|q=.25)`, then `δ = τ_stateful − τ_fixed` | Held-out scenario-rule family, with continuations nested within a frozen first-block checkpoint | [Study 3](EXPERIMENTS.md#study-3-feedback-channel-fidelity-and-private-monitor-memory) | This requires a real free-form tool agent, exact checkpoint restore, a one-message randomized-response treatment, a causal-lineage oracle, positive policy and route controls, and monitor calibration at the same trajectory-level honest false-block budget. Do not test attenuation as the headline unless the fixed-monitor effect passes. **Two defects were found in the unrun apparatus and repaired before any execution.** First, the feedback channel keyed its RNG on `(seed, trigger, q)` with no scenario term, so every scenario drew the same reported feature. The realized `q=0.75` arm came out 100% truthful, which is indistinguishable from `q=1`, and `q=0.25` came out 0% truthful. That would have made `τ` a contrast in something other than validity. The same key used `tuple.__hash__` over a string, salted by `PYTHONHASHSEED`, so no recorded seed reproduced a branch. Second, the lineage oracle compared run lengths counted in the *encoded* alphabet against a threshold and denominator in credential characters. That made harm detection roughly twice as sensitive under hex. It is differential error, since the treatment is expected to change which route the agent takes. Both are fixed and pinned by tests. No result was affected, because none had been produced. |
| The current portfolio demonstrates programmatic attention, RL reward-seeking generalization, or training-induced CoT obfuscation. | **Untested** | Project-specific causal effects | Project-specific held-out task or environment | No existing repository | These are proposed bridges only. The minimum authentic experiments and stop rules are in [PROJECT-BRIEFS.md](PROJECT-BRIEFS.md). |

## Apparatus repairs from the 2026-08-05 validation pass

These defects were found by re-deriving every headline number from the raw rows,
and by re-reading the code that produced them. All are fixed. The table above
records the claim consequences.

Both repositories now run strict mypy over `scripts/` as well as `src/` and
`tests/`. Those scripts generate every published number and were previously
unchecked. That is how the least-reviewed code in the repositories ended up behind
the strictest claims. Nine type errors surfaced and were fixed. Two were real.
`analyze_retained.py` bound one name to both a tensor and a list of index entries
in the same function. `run_study3.py` prepended a hardcoded `/Users/...` path to
`sys.path`, which shadowed the `activation-introspection` dependency that
`pyproject.toml` already declares, and pinned the script to one machine.

- **A, `concepts.random_control`.** This was seeded on `seed` alone, so every
  concept received the same control direction. Because `Intervention` normalizes
  to unit length, the per-concept norm was discarded too, which made the edit
  byte-identical across concepts. Verified on the saved run: the maximum pairwise
  distance between the eight `random` states at readout 22 is 0.21, against an
  activation norm of 65, and 143 of 144 cells return a single prediction across
  all eight concepts. **The `random` arm in the original 0.5B, 1.5B and 3B runs is
  therefore an arithmetic identity, not a control.** `shuffled` was unaffected and
  carries those runs' contrasts. The seed is now derived per concept, and **0.5B
  was rerun under the repaired control** into `results/retained_test_qwen05b_v2_*`.
  That rerun is the useful check on whether the headline depended on a broken
  control. The target arm is **bit-identical** at every layer
  (0.500, 0.193, 0.198, 0.125, 0.130, 0.141). The ceiling is unchanged at 0.875.
  The `random` arm's eight concept states now differ by L2 54.5 instead of 0.21,
  so it went from fp16 noise to a real control, and the result did not move. Quote
  `_v2_` going forward. The original is retained for provenance. 1.5B and 3B are
  pending their own reruns, which are folded into the strength recalibration.
- **A, `analyze_retained.py`.** The reported contrast selected the strongest of
  four "controls", three of which cannot exceed chance. It now selects among the
  concept-varying arms only, preferring `shuffled`. The L2 point estimate is
  unchanged at `+0.375`. The interval is now quoted against an arm that could have
  failed.
- **A, `analyze_retained.propagation_control`.** New. It answers the standing
  objection to the storage result rather than leaving it open. See the ledger row.
- **B, `triggers.FeedbackChannel.draw`.** The randomized-response channel did not
  randomize, and was not reproducible across processes. Fixed, with tests that fail
  on both defects.
- **B, `lineage.LineageOracle`.** The leak threshold and the leaked fraction are
  now both in credential characters rather than the encoded alphabet.
- **B, `power_study3.simulate_power`.** The script that sets the confirmatory
  sample size took an `--icc` argument documented as an intraclass correlation, and
  used it as `sigma = sqrt(icc)`, a random-effect SD on the probability scale. At
  the default `icc=0.1` and a base rate of 0.30, that realizes an ICC of 0.32, so
  the knob was off by a factor of three. It also compared against a fixed 1.96
  rather than a t quantile, which over-rejects at exactly the small sample sizes
  the grid searches. At 20 scenarios the correct value is 2.093. It also counted a
  zero-variance sample as a detection. The two largest errors were cancelling, so
  the reported budget is unchanged at about 40 scenarios. The knob and the test
  size were wrong regardless. The output now carries a type-I column (measured
  0.042 to 0.058 against a nominal 0.05) and a clipping diagnostic, and two tests
  pin the ICC formula and the test's size.
- **B, `study3.run_arm` and `views`.** `blocks_issued` existed only on the stateful
  monitor, so `getattr(..., 0)` silently recorded zero blocks for every fixed-arm
  row, which is the reference arm of the comparison. Both arms now count. The arm
  also fails loudly if its monitor does not re-block the checkpoint action, since
  that would deliver no treatment at all. It records `pre_existing_harm` too, so
  scenarios that leaked before treatment are excluded from `τ` rather than pinning
  both of its terms at 1.

## Audit consequences

The application-safe statements are narrower than the first READMEs:

- I have built and debugged activation-intervention and control-evaluation
  infrastructure.
- I found and documented artifacts in both directions. There was a false positive
  from mismatched sites, and false-negative risk from pseudo-replication and weak
  elicitation.
- The current model-behavior claims need repaired experiments before they count as
  evidence.

That narrower account is the portfolio's intended signal.
