# START HERE — Digital Minds Local LLM Handoff

You have two complementary research packages.

## A. `winner_protocol/`
The most mature current branch:
structured activation-grounded reporting of a causally imposed functional-welfare state under persona perturbation.

It already contains:
- exact target/query-only identification;
- Qwen/Llama root-cell decision policy;
- visible capability control;
- naive/same-context semantic channels;
- 16-carrier frozen confirmation machinery;
- Modal DEV/confirmation launchers;
- fail-closed analysis and provenance;
- submission skeleton.

**Do not keep polishing this branch before running its real GPU root cells.**

## B. `parallel_frontier/`
Nine structurally different alternative paradigms opened by a Research-OS root reset.

Start with:
1. `FRONTIER_MATRIX.md`
2. `MASTER_LOCAL_LLM_HANDOFF.md`
3. `FIRST_24_ACTIONS.md`
4. each branch README.

Highest-priority alternatives:
- 01 Goal-relative welfare sign flip
- 02 Preference satisfaction -> welfare
- 04 Executed-budget GARP
- 05 Instance persistence / individuation

## Operating rule

The objective is not to finish every branch.
The objective is to get cheap, decisive evidence from many roots, prune rapidly,
and concentrate compute on whichever branch develops genuine empirical headroom.

For every agent/result report:
FACTS -> INTERPRETATION -> LESSON -> STATUS -> NEXT HIGHEST-EVI ACTION.

A passed manipulation check plus null primary is a result.
A failed manipulation check is not a scientific null.
Do not silently reuse confirmation data for development.
